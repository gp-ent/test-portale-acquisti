code: test-latest-bundle
description: Test latest bundle

components:
  plugins:
    - plugins/portaleacquisticdp-plugin.yaml

  widgets:
# entando-needle-descriptor-add-widgets - Entando blueprint will add widget bundle files here

